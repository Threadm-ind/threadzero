{
  "name": "threadzero",
  "version": "0.1.0",
  "private": false,
  "description": "Thin execution wrapper for coding agents",
  "bin": {
    "threadzero": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "dev": "tsx src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
